[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r backend/requirements-production.txt"]

[start]
cmd = "cd backend && python -m uvicorn api.main_production:app --host 0.0.0.0 --port ${PORT:-8000}"

[variables]
PORT = "8000"